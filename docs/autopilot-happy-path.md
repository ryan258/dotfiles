# Autopilot Boundary

The Cyborg autopilot happy path moved to the Cyborg sibling repo.

- Sibling repo: `~/Projects/cyborg-agent`
- Full guide: `~/Projects/cyborg-agent/docs/autopilot-happy-path.md`
- Preserved shortcuts: `ap`, `apy`, `apb`, `apby`, `apbp`, `apbpy`, `apc`

The dotfiles aliases still call `bin/cyborg`, which delegates to the sibling
repo when it is installed.
