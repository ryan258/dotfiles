# Cyborg Docs Sync Boundary

`cyborg-sync` moved to the Cyborg sibling repo.

- Sibling repo: `~/Projects/cyborg-agent`
- Full guide: `~/Projects/cyborg-agent/docs/cyborg-docs-sync.md`
- Dotfiles wrapper: `bin/cyborg-sync`
- Scoped site check wrapper: `scripts/cyborg_scoped_site_check.sh`

Existing commands and manifest references keep resolving through the dotfiles
wrappers. Missing sibling setup produces a short unavailable message.
