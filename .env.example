# AI Staff HQ Dispatcher Configuration
# Copy this file to .env and fill in your actual values

# OpenRouter API Key
# Get yours at: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Model configurations for each dispatcher
# See available models at: https://openrouter.ai/models

# Technical debugging dispatcher (dhp-tech.sh)
# Recommended: Fast, capable models like GPT-4o-mini or Claude Haiku
DHP_TECH_MODEL=openai/gpt-4o-mini

# Creative writing dispatcher (dhp-creative.sh)
# Recommended: Creative models like GPT-4o or Claude Sonnet
DHP_CREATIVE_MODEL=openai/gpt-4o

# Content strategy dispatcher (dhp-content.sh)
# Recommended: Balanced models like GPT-4o or Claude Sonnet
DHP_CONTENT_MODEL=openai/gpt-4o

# Strategy dispatcher (dhp-strategy.sh) - defaults to content model if not set
# Recommended: Same as content model for consistency
# DHP_STRATEGY_MODEL=openai/gpt-4o

# Optional AI Features (set to "true" to enable)
# These features consume API credits when enabled

# AI Daily Briefing in startday.sh
# Provides AI-generated focus suggestion based on journal and tasks
# Caches result for the day to avoid multiple API calls
AI_BRIEFING_ENABLED=false

# AI Daily Reflection in goodevening.sh
# Provides AI-generated reflection on day's accomplishments and tomorrow's focus
AI_REFLECTION_ENABLED=false
